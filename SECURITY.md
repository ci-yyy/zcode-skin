# 安全说明（SECURITY）

## 威胁模型

本工具的攻击面主要来自两个本地端口：

- **9343（ZCode 调试端口）**：由 ZCode 自身提供，本工具只是使用方。CDP 协议**没有身份认证**。
- **9344（守护进程主题服务）**：本工具的 daemon.mjs 提供，同样**没有身份认证**。

两个端口都只绑定 `127.0.0.1` 本机回环，不对外网开放。但同一台电脑上以**同一用户权限**运行的
任何进程（包括你运行的其他软件、浏览器里被 XSS 的页面通过 localhost 访问等场景）都能连接它们：

- 连上 9343 可以读取/控制 ZCode 渲染进程（执行任意 JS）
- 连上 9344 可以切换主题、上传图片生成主题文件（写入范围限定在工具目录 `themes/` 内，文件类型限定 PNG/JPG/WebP，大小限 12MB）

## 缓解措施

- 守护进程仅监听回环地址，不提供任意文件读写；上传目录名校验拒绝 `..` 穿越
- 注入的 CSS 只覆盖 CSS 变量，不执行远程代码、不请求外部网络
- 工具不修改 ZCode 安装目录、签名、会话数据；随时可整体移除

## 如果你在意这个风险

1. 关闭主题中心：`node apply.mjs --remove-panel`，再 `bash uninstall-daemon.sh`
2. 完全退出 ZCode，从启动台/访达正常打开（调试端口消失，风险窗口关闭）

## 报告安全问题

请通过 GitHub 仓库的私有渠道联系维护者，不要公开提交 Issue。
